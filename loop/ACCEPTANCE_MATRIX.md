# Acceptance Matrix

| Req ID | Title | Priority | Target Milestone | Status | Verification Evidence |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **SSD-B6** | Return-basis, index parsing, and disclosure remediation G1–G5 | Critical | Stock-score diagnostic | **PARTIAL — G4 FAILED; SAMPLE INSUFFICIENT** | G1 fixes constant-zero `market_1d` (35,142 rows, 55 unique values) while preserving executable next-open returns. G2 adds a separate aligned close-to-close table and quantifies entry-day open-to-close bias (mean -0.2101%, median -0.2079%, 18.42% of the legacy negative decile result). G3 rejects the total-return index with red/green proof. G4 made exactly one TWSE request, failed with `NullReferenceException`, did not retry, and added no snapshot/sample; wide dates are 3/1/0/0/0. G5 reports narrow realized-horizon `UNADJUSTED` at 2,224/23,938 = 9.29%, removes unused `os`, and adds daily D10-D1 Newey-West stats. Actionable 10d result remains non-decisive (+0.466pp; rho .079; daily t 1.8166, n=43), and score Top-10 trails volume Top-10 at 5/10/20d. Three new regressions; full suite **523 passed**. Protected/raw/processed/output manifests unchanged; no commit. Evidence: `Quant-Agent/_workbench/out/stock_score_diagnostic_2026-07-29/REPORT.md`, `loop/evidence/test_logs/pytest_batch6_run_log.txt`. |
| **SSD-B5** | Diagnostic correctness remediation F1–F5 | Critical | Stock-score diagnostic | **COMPLETE — SAMPLE INSUFFICIENT** | Per-return-date breadth gate; date-guarded FinMind+official TAIEX (66 rows through 2026-07-24, 7/18 and 7/20 rejected); calendar-contiguous stock paths; FinMind+TWSE+old/new TPEx merge (7/16 coverage 566/566); 845 nonpositive rows removed, including expected FinMind 804 rows/154 files, output residual 0. Full `gross_10d` max 1.0357 and <=-90% count 0. Wide-universe dates at 1/3/5/10/20d are 4/2/0/0/0, so headline is **「樣本不足，不可裁決」**. Score Top-10 trails volume Top-10 at 5/10/20d. Nine new regressions; full suite **520 passed**. Protected/raw/processed/output manifests unchanged; zero network, no commit. Evidence: `Quant-Agent/_workbench/out/stock_score_diagnostic_2026-07-28/REPORT.md`, `loop/evidence/test_logs/pytest_batch5_run_log.txt`. |
| **SSD-B4** | Disk-only stock-score diagnostic remediation | Critical | Stock-score diagnostic | **COMPLETE — SAMPLE INSUFFICIENT** | P0 official TWSE/TPEx adapter spans 67 disk snapshots through 2026-07-24 and 40,599 panel stock-days; no fetcher import/network call, raw snapshot hash/mtime unchanged, and `gross_10d <= -0.9` = 0. P1 median requires >=30 names, zero closes are null, and Newey-West lag is `max(auto, horizon-1)`. P2 headline uses FULL only and is **「樣本不足，不可裁決」**: min decile n 2,151 (<10,000), min daily n 48 (<50), 39 days (<40), and 10/20d wide-universe maturity is zero. 15 diagnostic tests; full suite **511 passed**. Evidence: `Quant-Agent/_workbench/out/stock_score_diagnostic_2026-07-28/REPORT.md`, `loop/evidence/test_logs/pytest_batch4_run_log.txt`. |

| Req ID | Title | Priority | Target Milestone | Status | Verification Evidence |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **SSD-01** | Read-only stock-score predictive diagnostic | Critical | Stock-score diagnostic | **BLOCKED** | All four tables, T+1 entry, overlapping portfolios, daily-series Newey-West stats, 1,000-draw random benchmark, and 8 unit tests implemented.  The rank rule passes (10-day D10-D1 +1.206pp; Spearman .697), but the disk premise is false: score snapshots contain 568, not ~1,950, stocks; mature deciles are 2,917–2,964, below required 10,000. No weights or production files changed. Evidence: `Quant-Agent/_workbench/out/stock_score_diagnostic_2026-07-28/REPORT.md`. |
| **DRF-02** | Second-batch safety closeout: observe fail-open, config mirror, M3 output isolation | Critical | Daily-report field | **PASSED** | E was first rejected (dead config: YAML values never reached `build_observe_rankings`, so changing the YAML did not change the real output). After rework, `run_pipeline` reads `observe_rankings.ranking_depth`/`sector_top_n` from `ConfigManager` and passes them through. Adversarial retest (real YAML edit, no ConfigManager patching, isolated output_dir): 300/5 -> 300/5; **50/2 -> 50/2**; block removed -> fallback 300/5. Default-value rankings JSON is sha256-identical to the live `outputs/observe/rankings_2026-07-16.json` (behaviour unchanged). D: fail-open build/write paths each turn red when sabotaged. F: no new mocks, `run_pipeline` still really runs, 7/16 workbook sha256+mtime unchanged across two consecutive runs. Full suite **496 passed**. Reports: `Quant-Agent/_workbench/out/moneyflow_secondbatch_verification_20260728.md`, `..._e_redo_verification_20260728.md`. |
| **DRF-01** | Daily-report field safeguards and 2026-07-20 official backfill | Critical | Daily-report field | **BLOCKED** | A/C implemented; full suite 493/493 passed. Official MI_INDEX recovery wrote 1,371 TWSE rows with self-reported date guards. Required independent TWSE `STOCK_DAY?date=20260720&stockNo=2330` check instead returned 2026-07-27, so the existing 7/20 FinMind-fallback audit was not overwritten or relabelled as official. |
| **M0-01** | Create 29 required directories | Critical | Milestone 0 | **PASSED** | Checked directory layout |
| **M0-02** | Design Contracts & Schemas | High | Milestone 0 | **PASSED** | Files in `docs/` |
| **M0-03** | OpenAPI Caching (Real responses) | High | Milestone 0 | **PASSED** | 6 JSON files with metadata cached in `loop/evidence/raw_samples/` |
| **M0-04** | Fix Random Seed for reproducibility | High | Milestone 0 | **PASSED** | `create_demo_data.py` uses `np.random.seed(42)` |
| **M0-05** | Historical reports analysis | High | Milestone 0 | **PASSED** | Compiled findings in `docs/historical_reports_analysis.md` |
| **P0-01** | Full-market daily prices as source | Critical | Milestone 1 | **PASSED** | Verified in `tests/integration/test_run_daily.py` (entrypoint mock) |
| **P0-04** | Stock ID prefix zero string | High | Milestone 1 | **PASSED** | Verified in `tests/unit/test_data_cleaner.py` (padded tickers) |
| **P1-03** | Data Quality Score engine | High | Milestone 1 | **PASSED** | Verified in `tests/unit/test_data_validator.py` (blocked status triggered) |
| **C07** | Leaderboard Reconciliation | Medium | Milestone 1 | **PASSED** | Verified in `tests/integration/test_m1_integration.py` (deviation checks) |
| **P0-03** | Future data leakage test | Critical | Milestone 1 | **PASSED** | Verified in `tests/acceptance/test_future_leakage.py` |
| **P0-02** | Success/Failure labels | High | Milestone 2 | **PASSED** | `src/labels.py` + `tests/unit/test_labels.py` (9 tests: success/minor-failure/reversal thresholds, insufficient-data on None/NaN, fade override). Labels only; no backtest run (out of M2 scope per SPEC_ADDENDUM A-2) |
| **P0-05** | No volume double-counting | High | Milestone 2 | **PASSED** | `src/sector_features.py` `may_double_count` flag; `tests/unit/test_sector_features.py::test_primary_sector_volume_no_double_counting_p0_05` (primary turnover sums exactly to market total) and `::test_theme_aggregation_flags_may_double_count_true` (themes flagged True); also re-verified end-to-end in `tests/integration/test_m2_e2e_pipeline.py` on persisted CSV output |
| **P0-06** | Backtest limit-up lockouts | High | Milestone 3 | **Pending** | Backtester pipeline integration pending; correctly out of scope this milestone (P0-06 is a backtest-milestone item, not M3 signal/Excel scope) |
| **P1-01** | Dynamic weight renormalization | High | Milestone 2 | **PASSED** | `src/sector_scoring.py` and `src/stock_scoring.py`; `tests/unit/test_sector_scoring_confidence.py` (7 tests incl. renormalization sums to 1.0, LOW confidence when all factors missing) and `tests/unit/test_stock_scoring.py` (6 tests). Verified end-to-end via `tests/integration/test_m2_e2e_pipeline.py` and real-snapshot integration test |
| **P1-02** | Excel Slim Output (4 sheets) | Medium | Milestone 3 | **PASSED** | `src/report_generator.py` rewritten to produce exactly `["Dashboard", "新起漲族群", "續漲族群", "個股優先排序"]` per SPEC_ADDENDUM B-4; verified via `openpyxl.load_workbook` reopen in `tests/unit/test_report_generator.py::TestFourSheetsOnly` (synthetic data) AND `tests/integration/test_m3_real_snapshot_e2e.py` (real 2026-07-16 snapshot, real file at `outputs/daily/MoneyFlow_Rotation_2026-07-16.xlsx`, 13,671 bytes) |
| **M2-01** | Stock-level rolling features (rank, volume, 20d-high distance) | High | Milestone 2 | **PASSED** | `src/stock_features.py::calculate_rolling_features`; `tests/unit/test_stock_features.py` (5 tests incl. min_periods NaN enforcement, no-future-leakage truncated-vs-full regression, per-stock independence) |
| **M2-02** | Sector breadth/concentration/relative-strength features | High | Milestone 2 | **PASSED** | `src/sector_features.py`; `tests/unit/test_sector_features.py` (8 tests) |
| **M2-03** | Sector scoring model (25/25/20/15/10/5, PLACEHOLDER-marked) | High | Milestone 2 | **PASSED** | `src/sector_scoring.py`; unit tests as above |
| **M2-04** | Stock scoring model (35/20/15/10/10/10, PLACEHOLDER-marked) | High | Milestone 2 | **PASSED** | `src/stock_scoring.py` (new file); unit tests as above |
| **M2-05** | Lifecycle classification (6-stage, 3/5/10-day evidence, insufficient-data fallback) | High | Milestone 2 | **PASSED** | `src/lifecycle_classifier.py`; `tests/unit/test_lifecycle_classifier.py` (8 tests incl. <3-day INSUFFICIENT_DATA, exactly-3-day PARTIAL, >=10-day FULL) |
| **M2-06** | Overheat risk score (0-100) | Medium | Milestone 2 | **PARTIAL** | Implemented in `src/sector_scoring.py::_compute_overheat_risk` using only 3 of ~9 spec sub-factors (breadth/volume divergence, volume-surge proxy, HHI concentration); consecutive-limit-up count, upper-shadow-candle ratio, and institutional-selling-reversal factors NOT implemented (no data source wired for those yet). Bounded 0-100 and clamp-tested but sub-factor completeness is a known limitation, disclosed in `docs/Milestone_2_Acceptance_Report.md` |
| **M2-07** | Industry mapping template generator | High | Milestone 2 | **PASSED** | `scripts/build_mapping_template.py`; manually verified against real `loop/evidence/raw_samples` snapshots (1,988 discovered, 1,980 emitted as unclassified, 8 already mapped correctly excluded). No automated pytest test written for this script (manual verification only) |
| **M2-08** | Pipeline wiring: multi-day processed feature/score output | High | Milestone 2 | **PASSED** | `scripts/run_daily.py` persists `data/processed/*.csv` per trade_date and rebuilds rolling history strictly from files dated <= current run (structural no-future-leakage); `tests/integration/test_m2_e2e_pipeline.py` |
| **M3-01** | New Gainer 10-condition signal detection (SPEC Ch.14), A/B/C grading | High | Milestone 3 | **PASSED** | `src/signal_detector.py::_evaluate_new_gainer`; `tests/unit/test_signal_detector.py` (`TestNewGainerAGrade`, `TestBGrade`, 4 tests) |
| **M3-02** | Continued Momentum signal detection (SPEC Ch.15) | High | Milestone 3 | **PASSED** | `src/signal_detector.py::_evaluate_continued_momentum`; `TestContinuedMomentum` (3 tests) |
| **UAT-04** | Single-stock spike must not be classified sector-level new-gainer | Critical | Milestone 3 | **PASSED** | `MIN_UP_STOCKS_FOR_SECTOR_SIGNAL` hard gate in `src/signal_detector.py::_grade_new_gainer`; `TestUAT04SingleStockSpike` (2 tests: capped at C for up_stock_count=1, NOT capped at exactly 2) |
| **M3-03** | Per-condition pass/fail/unevaluable + invalidation condition + data confidence on every signal | High | Milestone 3 | **PASSED** | `conditions_passed/failed/unevaluable`, `invalidation_condition`, `signal_data_confidence` columns; `TestDataDegradation` (5 tests: missing institutional data, missing DQ score, low mapping coverage, no prior-day data) |
| **M3-04** | All signal thresholds config-managed, PLACEHOLDER-marked | High | Milestone 3 | **PASSED** | `config/default.yaml` `new_gainer.*`/`continued_momentum.*` (15 keys, each commented with its SPEC Ch.14/15 rule number); mirrored in `src/config_manager.py::get_defaults` |
| **M3-05** | Real 2026-07-16 snapshot end-to-end -> real Excel in outputs/daily/ | Critical | Milestone 3 | **PASSED** | `tests/integration/test_m3_real_snapshot_e2e.py`; real file `outputs/daily/MoneyFlow_Rotation_2026-07-16.xlsx` (13,671 bytes), no network calls, no synthetic data. See `docs/Milestone_3_Acceptance_Report.md` §4 for a disclosed pre-existing M1 environment-coupling finding (external leaderboard glob path + return-basis mismatch) encountered and worked around (not silently fixed) while producing this evidence |
| **M3-06** | Execution log + audit JSON per run | Medium | Milestone 3 | **PASSED** | `scripts/run_daily.py::_setup_run_logger`/`_write_audit_summary`; real `outputs/logs/run_2026-07-16.log` and `outputs/logs/audit_2026-07-16.json` produced by the real-snapshot run above |
| **M3-07** | `docs/signal_definitions.md` expanded with grade/condition/invalidation detail | Medium | Milestone 3 | **PASSED** | New §3.5 (10-condition table, 9-condition table, UAT-04 description, track precedence) |
| **M4-01** | Daily fetcher: TWSE+TPEx OHLCV/institutional/margin, fail-closed retry, idempotent `.bak` | Critical | Milestone 4 | **PASSED** | `src/data_fetcher.py::DataFetcher`, `scripts/fetch_daily_data.py`; `tests/unit/test_data_fetcher.py` (15 tests: HTTP 200/404/empty-payload/schema-error/retry-exhausted paths, idempotent backup, filename format) |
| **M4-02** | Market index fetch: TAIEX (TWSE MI_INDEX) + 櫃買指數 (TPEx tpex_index), real verified endpoints | High | Milestone 4 | **PASSED** | `src/data_fetcher.py::ENDPOINTS["market_index"]`, verified against cached `loop/evidence/raw_samples/{twse,tpex}_swagger.json`; live-fetched 2026-07-17 (267 + 12 rows, HTTP 200) — see `loop/evidence/fetch_receipts/fetch_receipt_2026-07-17.json` |
| **M4-03** | `--backfill start end` range fetch, weekends skipped, holiday-empty-payload treated as normal | High | Milestone 4 | **PASSED** | `src/data_fetcher.py::DataFetcher.backfill`; `tests/unit/test_data_fetcher.py::test_backfill_skips_weekends`, `test_is_holiday_response_*` |
| **M4-04** | `--smoke` live single-endpoint schema-validation mode | Medium | Milestone 4 | **PASSED** | `scripts/fetch_daily_data.py::run_smoke`; executed live: HTTP 200, 1,371 rows (`loop/evidence/fetch_receipts/smoke_receipt_2026-07-17.json`) |
| **M4-05** | Market regime 6-state classification (SPEC Ch.18), index-unavailable degradation, insufficient-data path | High | Milestone 4 | **PASSED** | `src/market_regime.py::MarketRegimeClassifier`; `tests/unit/test_market_regime.py` (10 tests: all 6 states, index-unavailable->breadth-only DEGRADED, <60-day history->INSUFFICIENT_DATA, extreme-risk override on volatility/drawdown) |
| **M4-06** | Institutional features: 3/5/10/20d cumulative, consecutive-buy streak, buy-pct-of-volume, foreign+trust same-direction | High | Milestone 4 | **PASSED** | `src/institutional_features.py::InstitutionalFeatures`; `tests/unit/test_institutional_features.py` (13 tests, hand-calculated cumulative sums and streaks, min_periods enforcement, Null discipline) |
| **M4-07** | C-1: investment-trust quarter-end (3/6/9/12 month) last-5-trading-day window flag | Medium | Milestone 4 | **PASSED** | `src/institutional_features.py::flag_quarter_end_window`; `test_quarter_end_window_flag_*` (boundary-tested exact 5th/6th day) |
| **M4-08** | Sector-level institutional aggregation (net-buying count, net buy total, pct of turnover) | Medium | Milestone 4 | **PASSED** | `src/institutional_features.py::aggregate_sector_institutional`; unit tests + attached to `sector_features_<date>.csv` in `scripts/run_daily.py` |
| **M4-09** | Margin features: balance change rate (3d/5d), usage rate (real or documented proxy), short-margin ratio | High | Milestone 4 | **PASSED** | `src/margin_features.py::MarginFeatures`; `tests/unit/test_margin_features.py` (8 tests, hand-calculated, Null discipline on zero/missing denominators) |
| **M4-10** | Institutional/margin feature wiring into `run_daily.py` pipeline + stock/sector scoring "institution" factor | Critical | Milestone 4 | **PASSED** | Closed a pre-existing gap: `foreign_net_buy` was never merged onto the stock-features frame before `stock_scoring.score_stocks`, so `score_institution` silently used a neutral 50.0 prior every day. Fixed in `scripts/run_daily.py`; proven by `tests/integration/test_m4_institutional_wiring_e2e.py` (real 2026-07-16 snapshot, asserts `foreign_net_buy`/`score_institution` are actually non-null) |
| **M4-11** | Live smoke test + real single-day fetch actually executed (not just claimed) | Critical | Milestone 4 | **PASSED** | `loop/evidence/fetch_receipts/smoke_receipt_2026-07-17.json` (HTTP 200) and `fetch_receipt_2026-07-17.json` (all 8 category×market endpoints, HTTP 200, sha256-verified against real `data/raw/*` files). This dev sandbox's outbound network was intermittently DNS-sinkholed on some invocations (retry/fail-closed logic correctly prevented any partial/corrupt overwrite); full root-cause disclosed in `docs/Milestone_4_Acceptance_Report.md` §5 |
| **M5a-01** | Official industry classification auto-import (TWSE + TPEx company basic data), never overwrite manually-reviewed rows, never guess unmapped stocks | Critical | Milestone 5a | **PASSED** | `scripts/build_official_mapping.py`; live-executed: TWSE `/opendata/t187ap03_L` 1,090 rows, TPEx `/mopsfin_t187ap03_O` 891 rows; coverage 0.41% (8/1974) -> 98.58% (1946/1974) against real 2026-07-17 universe; `tests/unit/test_build_official_mapping.py` (18 tests: reviewed-row protection, merge semantics, coverage metrics, fetch failure handling) |
| **M5a-02** | Industry code -> Chinese name resolution or honest UNAVAILABLE disclosure | High | Milestone 5a | **PARTIAL (honestly disclosed)** | Neither TWSE nor TPEx swagger exposes a code->name lookup endpoint (verified by search, not assumed); raw numeric codes (e.g. "28") kept as `primary_sector` for the 1,955 newly-imported rows, `industry_code_lookup_status=UNAVAILABLE` recorded in the receipt and visible as-is in the real 7/17 Dashboard -- not hidden, not invented |
| **M5a-03** | Payload date-consistency validation: drop (never save) a fetched payload whose self-reported date doesn't match the requested trade_date | Critical | Milestone 5a | **PASSED** | `src/data_fetcher.py::extract_payload_date`/`_parse_roc_or_iso_date`, wired into `fetch_and_save` as a `DATE_MISMATCH` fail-closed path; `tests/unit/test_data_fetcher.py` (7 new tests: ROC/top-level date extraction, no-date-signal endpoints correctly NOT blocked, mismatched payload dropped and not written to disk) |
| **M5a-04** | Historical endpoint capability honestly discovered via swagger (not guessed) | Critical | Milestone 5a | **PASSED** | Grep-verified: TWSE `STOCK_DAY_ALL`/`MI_MARGN`/`MI_INDEX` and every TPEx OpenAPI path have zero query parameters (latest-day-only); only TWSE T86 (legacy RWD, outside the OpenAPI swagger) accepts `date=`, live-confirmed by requesting 2026-06-01 and observing the payload echo `date: "20260601"` |
| **M5a-05** | Resumable `--backfill` (skip already-fetched dates on re-run) | High | Milestone 5a | **PASSED** | `DataFetcher.fetch_and_save`/`fetch_all_categories`/`backfill` gained an additive `skip_existing` param (default False, preserves exact M4 behavior); CLI defaults `--backfill` to resumable, `--no-resume` opts out; `tests/unit/test_data_fetcher.py` (5 new tests incl. corrupt-existing-file re-fetch, partial-run resume) |
| **M5a-06** | Historical backfill 2026-04-20 to 2026-07-17 actually executed | High | Milestone 5a | **PARTIAL (disclosed)** | Attempted live; this session's sandbox network experienced a SUSTAINED (not intermittent) multi-hour total outage to both TWSE and TPEx hosts (`Resolve-DnsName` confirmed sinkhole to `10.0.0.1`). Actual per-category/market success-day counts in `docs/Milestone_5a_Acceptance_Report.md` and `loop/evidence/fetch_receipts/backfill_summary.json` -- reported honestly, not padded |
| **M5a-07** | `scripts/run_history_pipeline.py`: sequential batch feature/score/signal run over backfilled dates, one bad day doesn't abort the batch | High | Milestone 5a | **PASSED** | Reuses `scripts.run_daily.run_pipeline` (given an additive return value); `tests/unit/test_run_history_pipeline.py` (12 tests: date discovery requiring both markets, prev_date threading, per-day JSONL writing, blocked-day-continues-batch) |
| **M5a-08** | `outputs/signals/signals_<date>.jsonl` per-day signal event log | Medium | Milestone 5a | **PASSED** | `scripts/run_history_pipeline.py::_write_signals_jsonl`; one JSON object per sector-signal row (both hits and non-hits, for future event-study denominators); format-tested |
| **M5a-09** | Real 2026-07-17 report produced end-to-end from real fetched data with official mapping applied | Critical | Milestone 5a | **PASSED** | `outputs/daily/MoneyFlow_Rotation_2026-07-17.xlsx`; audit status=SUCCESS, DQ=91.0/WARNING, coverage=98.58%, 44 sectors (29 B級早期點火 + 15 C級個股事件), 1974 stocks scored. Bridged via new `scripts/prepare_legacy_raw_snapshot.py` (M4 filename -> legacy filename `run_pipeline` reads, pure copy, disclosed pre-existing M4 naming gap) |
| **M5a-10** | `tests/integration/test_m3_real_snapshot_e2e.py` low-coverage-warning assertion made conditional on actual coverage, not hardcoded | Critical | Milestone 5a | **PASSED** | Test now reads `mapping_coverage_pct` from the real audit JSON and asserts the warning only when `<80%`, and that the real percentage displays when `>=80%`; all other pre-existing assertions (4 sheets, uncalibrated-threshold warning, etc.) unchanged |
| **M5b-01** | FinMind historical fetcher: dataset names dry-run verified (not recited), fail-closed, rate-limit-aware, resumable | Critical | Milestone 5b | **PASSED** | `src/finmind_fetcher.py`/`scripts/fetch_history_finmind.py`; dry-run transcript `loop/evidence/fetch_receipts/finmind_dataset_probe_2026-07-18.json`; `tests/unit/test_finmind_fetcher.py` (27 tests: 402/429 short-circuit, empty-data-is-success, skip_existing exact-range match, TAIEX-works/OTC-unavailable, backfill_universe fail-closed-per-item + stop-on-rate-limit) |
| **M5b-02** | Real historical backfill executed 2026-04-20 to 2026-07-17, honestly reported including partial completion | High | Milestone 5b | **PARTIAL (disclosed)** | 571/1963 stocks' OHLCV (29.1%, full 62-day history) before FinMind's hourly quota was hit (HTTP 402, confirmed genuine via a resume probe); institutional/margin 0/1963 (quota exhausted first); OHLCV explicitly prioritized per instruction. Receipt `loop/evidence/fetch_receipts/finmind_backfill_summary.json`; resumable via default `skip_existing=True` |
| **M5b-03** | TPEx/OTC market index availability honestly determined via dry-run, not guessed | Medium | Milestone 5b | **PASSED (UNAVAILABLE, disclosed)** | 8 candidate `data_id`s tried (OTC/TPEX/TWO/OTC50/Y9999/IX0043/IX0044/TWOTCI), all HTTP 200 with zero rows; `TaiwanStockInfo` itself lists only TAIEX as an index entry. `OTC_INDEX_UNAVAILABLE` marker, never fabricated; `tests/unit/test_finmind_fetcher.py::test_fetch_market_index_tpex_returns_none_unavailable_not_fabricated` |
| **M5b-04** | Industry code -> Chinese name conversion via FinMind, reviewed rows protected, code preserved | Critical | Milestone 5b | **PASSED** | `scripts/build_chinese_sector_mapping.py`; 1,955/1,955 eligible rows converted (100%), 8 reviewed rows untouched, `sector_code` column preserves the old numeric code; dedup rule (max-date wins, same-date tie keeps first occurrence) verified exact against reviewed ground-truth rows; `tests/unit/test_build_chinese_sector_mapping.py` (10 tests, incl. real end-to-end `.xlsx` backup regression test) |
| **M5b-05** | Real 7/17 report re-run and verified to display Chinese sector names, old report backed up | High | Milestone 5b | **PASSED** | `outputs/daily/MoneyFlow_Rotation_2026-07-17.xlsx` regenerated (old version at `.xlsx.bak`); Dashboard top-10 table confirmed showing real Chinese names (金融保險/食品工業/紡織纖維/etc) in place of raw numeric codes; DQ/coverage unchanged (91.0/98.58%) since only the sector *label* changed |
| **M5b-06** | `data_loader.py` dual-source integration: FinMind readers + official-priority merge, source always distinguishable | High | Milestone 5b | **PASSED** | New `load_finmind_ohlcv_for_date`/`load_finmind_institutional_for_date`/`load_finmind_margin_for_date`/`merge_*_sources` methods (additive, no existing method changed); official rows always win on conflict, FinMind fills gaps only, `source` column never blended; `tests/unit/test_data_loader_finmind.py` (15 tests) |
| **M5b-07** | Historical batch pipeline extended to consume FinMind data via `--use-finmind`, official priority preserved at file level | High | Milestone 5b | **PASSED** | New `scripts/prepare_finmind_legacy_snapshot.py` (never overwrites an official-sourced legacy file) + `discover_finmind_dates`/`bridge_finmind_dates`/`use_finmind` flag in `scripts/run_history_pipeline.py` (default off, exact M5a behavior preserved); `tests/unit/test_prepare_finmind_legacy_snapshot.py` (9 tests) + 7 new tests in `test_run_history_pipeline.py` |
| **M5b-08** | Historical batch pipeline actually run end-to-end over the FinMind-backfilled range | High | Milestone 5b | **PARTIAL (disclosed)** | 62 dates processed: 2 SUCCESS, 26 EXCEPTION, 31 BLOCKED_LOW_DQ, 3 BLOCKED_MISSING_MARKET. Root causes for all 3 non-success categories investigated and disclosed in `docs/Milestone_5b_Acceptance_Report.md` §6 (a pre-existing latent bug in already-accepted `scripts/run_daily.py` code newly surfaced by real multi-day execution, NOT fixed per governance rule #9; fail-closed correctly rejecting DQ-insufficient days; pre-existing demo-data files shadowing both bridges for 3 dates). Batch driver itself behaved correctly -- no bad day aborted the remaining days |
| **M5b-09** | Reconciliation spot-check: FinMind OHLCV vs official snapshot | Medium | Milestone 5b | **PASSED (scope-adjusted, disclosed)** | 569/571 FinMind-fetched stocks' 2026-07-17 close exact-matched (100%, 0 mismatches) against the real official snapshot; stock 2330 individually confirmed. Only 1 date (2026-07-17) has a real official raw snapshot on disk (the 7/16 file present was found to be synthetic demo data and correctly excluded) -- the instruction's "3 trading days" could not be fully satisfied due to real data-availability limits, disclosed rather than padded; the single available date was checked across the full 571-stock universe instead of just 3 stocks to compensate |
| **M5b-10** | Test suite: 180 baseline + all new tests green | Critical | Milestone 5b | **PASSED** | 248/248 passed (180 pre-existing + 68 new); `loop/evidence/test_logs/pytest_m5b_run_log.txt` |
| **M5c-prep-01** | Fix the M4 institutional-column merge-suffix bug disclosed (not fixed) in M5b, with a real regression test | Critical | Milestone 5c-prep | **PASSED** | `scripts/run_daily.py` drops stale institutional columns before merging fresh values (~line 411-441); `tests/integration/test_run_daily_two_day_merge.py` (new) directly reproduces the bug pre-fix (day 2's CSV missing a clean `foreign_net_buy` column, split into `_x`/`_y`) and confirms the fix post-fix (clean column, correct fresh value). Only this one call site touched |
| **M5c-prep-02** | Resolve the 3-day `BLOCKED_MISSING_MARKET` mock-file shadowing | High | Milestone 5c-prep | **PASSED** | `data/raw/ohlcv/prices_2026-07-14/15/16.json` moved to `data/test_fixtures/legacy_mock/`; verified via the full batch rerun that 2026-07-16 now SUCCEEDS and 2026-07-14/15 now reach genuine DQ evaluation instead of being shadowed out before evaluation |
| **M5c-prep-03** | Independent hermeticity issue found and fixed in `test_m2_e2e_pipeline.py` (mock stock IDs colliding with a real external leaderboard file) | Medium | Milestone 5c-prep | **PASSED** | Isolation-tested to confirm NOT caused by the mock-file move (reproduced identically with files temporarily restored to original location); fixed by mocking `load_excel_leaderboard` in this one test, matching the existing hermeticity pattern in `test_run_daily.py`; zero production code touched |
| **M5c-prep-04** | FinMind institutional/margin backfill resumed, rate-limit behavior honestly characterized | High | Milestone 5c-prep | **PARTIAL (disclosed)** | institutional 0->26/1963, margin 0->2/1963 (OHLCV unchanged 571/1963, deprioritized this round per instruction); new finding that the rate limit is a short (~30-90s) burst throttle, not the clean hourly window M5b assumed, confirmed by 6+ real resume attempts; session stopped after ~5-6 min per the "don't wait >30min" instruction. Receipt `loop/evidence/fetch_receipts/finmind_backfill_summary.json` |
| **M5c-prep-05** | Full 62-day historical batch pipeline rerun, before/after comparison | Critical | Milestone 5c-prep | **PASSED** | 60/62 SUCCESS (was 2), 0 EXCEPTION (was 26), 2 BLOCKED_LOW_DQ (was 31, now only 2 genuinely-thin dates), 0 BLOCKED_MISSING_MARKET (was 3); 2,765 total signal events; backups of pre-rerun processed/signals/audit files taken before overwrite (`data/processed/_bak_m5c_prep_20260718_143440/` etc); `loop/evidence/fetch_receipts/history_pipeline_summary_2026-07-18_m5c_prep_rerun.json` |
| **M5c-prep-06** | Test suite: 248 baseline + new test green | Critical | Milestone 5c-prep | **PASSED** | 249/249 passed (248 pre-existing + 1 new); `loop/evidence/test_logs/pytest_m5c_prep_run_log.txt` |
| **M5c-01** | Event-study backtest core: event extraction, entry price, limit-up lockout, forward returns, cost model | Critical | Milestone 5c | **PASSED** | `src/backtester.py` full rewrite; `tests/unit/test_backtester.py` (30 tests) + `tests/leakage/test_backtester_no_future_function.py` (3 tests); real 60-day run: 53 independent events, both grades underperform momentum baseline, n<30 both tiers (honestly reported as「無增量價值證據」, not softened). See `docs/Milestone_5c_Acceptance_Report.md` |
| **M5c-02** | Momentum-extension + random-sector-bootstrap benchmarks | High | Milestone 5c | **PASSED** | `src/benchmarks.py`; `tests/unit/test_benchmarks.py` (9 tests); real N=10,000 bootstrap run, seed=42 reproducible |
| **M5c-03** | No-future-function guarantee on the new backtester | Critical | Milestone 5c | **PASSED** | `tests/leakage/test_backtester_no_future_function.py`: truncated-vs-full-dataset byte-identical returns, appending future bars doesn't change already-realized returns, immature horizon reports PENDING never a fabricated return |
| **M6-01** | One-click unattended daily orchestrator (fetch -> bridge -> pipeline -> signals JSONL) | Critical | Milestone 6 | **PASSED** | `scripts/daily_orchestrator.py`; real live-network run 2026-07-17 end-to-end SUCCESS (exit code 0), reproducibility confirmed (byte-identical processed CSVs across 2 independent runs); `tests/integration/test_daily_orchestrator.py` (12 tests) |
| **M6-02** | Error recovery: network failure / API empty response / DQ black-out behaviors documented + tested (mock) | Critical | Milestone 6 | **PASSED** | `run_daily_orchestration`'s 3-step try/except contract, exit codes 1/2/3 distinguish scenarios; `TestFetchFailure`, `TestPartialFetchStillReachesPipeline`, `TestDQBlackout`, `TestUnexpectedExceptions` test classes; DQ black-out (`BLOCKED_LOW_DQ`) confirmed to produce no Excel report, only the audit JSON, per spec Ch.9 |
| **M6-03** | Real orchestrator run executed against live TWSE/TPEx (not just mocked) | Critical | Milestone 6 | **PASSED (with a genuine finding)** | 2026-07-18 run surfaced real DATE_MISMATCH on all TWSE/TPEx OHLCV endpoints (official data not yet published at fetch time — a real, expected data-availability gap, not a bug) which cascaded into a pre-existing `run_daily.py` `KeyError` bug (see M6-06); no data corrupted, no false report produced, confirmed via file-timestamp check on the prior good 2026-07-17 report |
| **M6-04** | Reruns Start-Process-detached FinMind drip backfill (per task brief: "PID 2924 已死") | High | Milestone 6 | **PASSED (corrected finding)** | PID 2924 verified STILL ALIVE (`Get-Process -Id 2924`, `StartTime` matches M5c's launch), OHLCV progressed 571/1963 -> 802/1963 between M5c and this verification. NOT restarted — the task's premise ("已死") was stale/incorrect at verification time; killing and relaunching a healthy, progressing process would be needlessly destructive. Correction documented in `docs/acceptance_report.md` and `loop/PROJECT_STATE.md` |
| **M6-05** | Operations manual (user-facing, non-engineer) | Critical | Milestone 6 | **PASSED** | `docs/operations_manual.md`: daily run steps, 4-sheet report reading guide (incl. mandatory "訊號等級=未校準研究參考,不是買賣指令" disclosure), error reference table, backfill progress check commands, FinMind quota characteristics, Windows Task Scheduler setup steps (not executed) |
| **M6-06** | README rewrite + reproducibility self-check per spec 28.1 | Critical | Milestone 6 | **PASSED** | `README.md` full rewrite (fixed stale M0-era `Quant-Agent\.venv` path); reproducibility self-check actually executed: same-date (2026-07-17) rerun via live network produced byte-identical `sector_scored_2026-07-17.csv`/`stock_scored_2026-07-17.csv` (`diff` empty) |
| **M6-07** | Final acceptance report per spec Ch.36 format, honest final verdict | Critical | Milestone 6 | **PASSED** | `docs/acceptance_report.md`: verdict = Software Ready (candidate) — P0 11/12 (1 disclosed pre-existing non-blocking issue), P1 20/20, Critical/High=0; Research Ready explicitly judged NOT MET (n<30, single bull-market regime, 29-41% historical coverage, uncalibrated thresholds, signal underperforms momentum baseline — all per spec §28.2's own 10-item checklist); Trading Decision Support Ready explicitly NOT MET (§28.3) |
| **M6-08** | Regression test documenting a real pre-existing bug found (not fixed, out of authorized scope) | High | Milestone 6 | **PASSED** | `tests/regression/test_run_daily_empty_market_crash.py` (2 tests): hermetically reproduces `run_daily.py::run_pipeline`'s `KeyError: 'market_type'` when both the M4 legacy bridge files are absent and the internal legacy live-fallback also returns empty — root-caused to `pd.concat([pd.DataFrame(), pd.DataFrame()])` producing zero columns |
| **M6-09** | `VERSION` + `CHANGELOG.md` final entry | Medium | Milestone 6 | **PASSED** | `VERSION` = `v1.0.0-software-ready-candidate`; `CHANGELOG.md` final entry covers all M6 additions and the disclosed-not-fixed bug |
| **M6-10** | Test suite: 291 baseline + new tests green | Critical | Milestone 6 | **PASSED** | 305/305 passed (291 pre-existing + 12 orchestrator + 2 regression); `loop/evidence/test_logs/pytest_m6_run_log.txt` |
| **M7-01** | Dry-run verify FinMind adjusted-price dataset; compute ex-dividend adjustment factors from a verified fallback if unavailable | Critical | Milestone 7 | **PASSED (fallback path)** | 6 candidate direct "adjusted price" dataset names all confirmed UNAVAILABLE (HTTP 400/422, live dry-run, receipt `loop/evidence/fetch_receipts/finmind_adjusted_price_probe_2026-07-18.json`); `TaiwanStockDividendResult` verified live and used instead (`src/price_adjuster.py`, backward-multiplicative factor, compounds across multiple events) |
| **M7-02** | Fetch adjustment factors for the FinMind-backfilled universe | High | Milestone 7 | **PARTIAL (disclosed)** | 516/890 stocks (58%) fetched before hitting FinMind's rate limit (same throttle family as M5b/M5c/M6); resume attempt got a different HTTP 403 failure mode, session stopped rather than burn more time; receipt `loop/evidence/fetch_receipts/price_adjustment_fetch_summary_2026-07-18.json` |
| **M7-03** | Backtester uses adjusted prices via config switch, UNADJUSTED stocks tagged not blended | Critical | Milestone 7 | **PASSED** | `config/default.yaml backtest.use_adjusted_prices` (default true); `scripts/run_backtest.py::apply_price_adjustment`; `src/backtester.py`/`src/benchmarks.py` themselves unmodified; 42.0% of the 890-stock universe UNADJUSTED this run, reported in `backtest_summary_2026-07-18.json` |
| **M7-04** | Disposition/attention stock endpoints discovered from swagger, fetcher built fail-closed | Critical | Milestone 7 | **PASSED** | 5 real endpoints found (TWSE `/announcement/punish`+`/announcement/notice`, 3 TPEx warning endpoints), all live-verified; `src/disposition_fetcher.py`; today's real list: 41 unique stocks (12 disposition, 29 attention); receipt `loop/evidence/fetch_receipts/disposition_today_list_2026-07-18.json` |
| **M7-05** | Disposition/attention flag wired into daily report + backtester's real `disposition_stock_ids` | High | Milestone 7 | **PASSED (same-day-only, disclosed)** | `scripts/run_daily.py::_load_disposition_ids_for_date` + `src/report_generator.py`'s new 處置/注意 column; `scripts/run_backtest.py::load_disposition_stock_ids` passes a real 41-stock set into `Backtester.run_event_study`'s pre-existing parameter; disclosed as a same-day snapshot only (no historical date-range query parameter exists on any of the 5 endpoints) |
| **M7-06** | 36-day user-collected leaderboard: limit-up count/streak history (proxy) at market + sector level | High | Milestone 7 | **PASSED (observe-only)** | `src/leaderboard_loader.py`+`src/limit_up_history.py`; all 36/36 files parsed; 2,502 limit-up stock-days, max streak 10 days (stock 8291); deliberately NOT wired into `sector_scoring.py`'s live overheat-risk score (already-accepted M2 formula, governance rule #9) — persisted as a disclosed, additive dataset for a future milestone's explicit activation decision |
| **M7-07** | 36-day cross-reconciliation, basis-mismatch-aware (leaderboard prev-close vs system's own open-to-close) | Critical | Milestone 7 | **PASSED** | `src/leaderboard_reconciliation.py` computes an independent FinMind prev-close-basis return rather than comparing mismatched bases directly; 40.0% coverage (4,318/10,800, limited by FinMind OHLCV progress), 0.97% of compared rows exceed the 0.5pp threshold; found+disclosed 3 genuine FinMind zero-price data-corruption rows along the way; a real zero-division bug (+inf instead of NaN) was caught and fixed in this module's own test-writing before the real run |
| **M7-08** | Backtest rerun with adjusted prices, old-vs-new headline comparison | Critical | Milestone 7 | **PASSED** | Old outputs backed up to `outputs/backtests/_bak_pre_m7/*.bak`; new run: headline medians UNCHANGED (B -11.28% n=27, C -10.41% n=19, both still lose to -0.39% momentum baseline, both still n<30); only 2/53 events changed (verified via direct event-level diff, confirming the adjustment mechanism is genuinely applied, just had limited effect on this specific sample) |
| **M7-09** | Test suite: 305 baseline + new tests green, zero regressions | Critical | Milestone 7 | **PASSED** | 361/361 passed (305 pre-existing + 56 new across 7 new files + 3 additive in `test_report_generator.py`); `loop/evidence/test_logs/pytest_m7_run_log.txt` |
| **M8-01** | Fix `run_daily.py` empty-market `KeyError('market_type')`, flip regression test to assert fixed behavior | Critical | Milestone 8 | **PASSED** | `scripts/run_daily.py` guards `"market_type" not in df_prices.columns` before indexing; falls through to pre-existing `BLOCKED_MISSING_MARKET`; `tests/regression/test_run_daily_empty_market_crash.py` renamed/reasserted |
| **M8-02** | `load_excel_leaderboard` path configurable via `config/default.yaml`, default preserves pre-M8 behavior | High | Milestone 8 | **PASSED** | New `reconciliation.leaderboard_dir` key (default = old hardcoded `C:/Workspace_CN/Quant-Agent`); missing dir logs+skips reconciliation; 5 new tests in `tests/unit/test_load_excel_leaderboard_config.py` |
| **M8-03** | `scripts/backfill_status.py`: direct-disk-scan backfill progress truth tool | High | Milestone 8 | **PASSED** | Counts `finmind_<stock_id>.json` per category vs live `stock_industry_mapping.xlsx` universe size; `--json` or human table; 8 new tests in `tests/unit/test_backfill_status.py` (tmp_path fixtures) |
| **M8-04** | First-ever `coverage` measurement (spec §28.1), package installed, no gate enforced | Medium | Milestone 8 | **PASSED (measure-only)** | `loop/evidence/test_logs/coverage_first_measurement.txt`: core `src/` 86%, whole project 80% — both meet the 85%/75% gates on this first measurement; `requirements.txt` annotated dev-only |
| **M8-05** | Governance registration: PID 9836 lineage, backfill-status-as-truth-source, KNOWN_ISSUES resolved items | Medium | Milestone 8 | **PASSED** | `loop/PROJECT_STATE.md` + `loop/KNOWN_ISSUES.md` updated this milestone |
| **M8-06** | Test suite: 361 baseline + new tests green, zero regressions | Critical | Milestone 8 | **PASSED** | 374/374 passed (361 baseline + 5 leaderboard-config + 8 backfill_status, net of 1 flipped regression-test assertion); `loop/evidence/test_logs/pytest_m8_run_log.txt` |
| **M7-10** | Untouched modules verification (backtester.py/benchmarks.py/sector_scoring.py unmodified) | Critical | Milestone 7 | **PASSED** | Zero lines changed in `src/backtester.py`, `src/benchmarks.py`, `src/sector_scoring.py` — verified by the fact all their existing tests pass unmodified and only caller-side (`scripts/run_backtest.py`) and additive (`config/default.yaml`) changes were made |
| **MHB-01** | TWSE/TPEx official free-endpoint margin history fetchers, fail-closed, date-consistency-checked | Critical | Margin History Backfill | **PASSED** | `src/twse_tpex_margin_history.py::fetch_twse_margin_history`/`fetch_tpex_margin_history`; `tests/unit/test_twse_tpex_margin_history.py`; found+fixed a real TWSE non-trading-day-misclassified-as-DATE_MISMATCH bug during the live run, regression-tested |
| **MHB-02** | ROC-year-slash date conversion for TPEx endpoint | High | Margin History Backfill | **PASSED** | `iso_to_roc_slash()`; parametrized tests incl. ROC 100/120 boundary and out-of-band rejection |
| **MHB-03** | Raw rows transformed into `clean_margin_data`'s existing expected format, zero cleaner changes | Critical | Margin History Backfill | **PASSED** | `transform_twse_margin_rows`/`transform_tpex_margin_rows`; end-to-end tests feed transformed output directly through the real (unmodified) `DataCleaner.clean_margin_data` and assert correct `margin_buy`/`margin_sell`/`margin_balance`/`short_buy`/`short_sell`/`short_balance` |
| **MHB-04** | Resumable backfill CLI, distinct filename prefixes, no existing file overwritten | Critical | Margin History Backfill | **PASSED** | `scripts/backfill_margin_history.py`; `twse_official_<date>.json`/`tpex_official_<date>.json`; `tests/unit/test_backfill_margin_history.py` (skip-existing, date-range, aggregation) |
| **MHB-05** | Real full-range backfill executed (2026-04-20..2026-07-20), free endpoint so full run allowed | Critical | Margin History Backfill | **PASSED** | 63/63 trading days saved both markets, 29 non-trading days skipped, 0 failures; `loop/evidence/fetch_receipts/margin_history_backfill_summary.json`; `docs/Margin_History_Backfill_Report.md` |
| **MHB-06** | `backfill_status.py` extended with per-date margin coverage, existing per-stock behavior unchanged | Medium | Margin History Backfill | **PASSED** | Additive `margin_date_sources` section; existing 8 tests still pass unmodified; 4 new tests |
| **MHB-07** | Test suite: 376 baseline + new tests green, zero regressions | Critical | Margin History Backfill | **PASSED** | 419/419 passed (376 baseline + 39 margin-history + 4 backfill_status); `loop/evidence/test_logs/pytest_margin_history_run_log.txt` |
| **M9-01** | Wire previously-orphaned official margin-history files into the live pipeline (not symbolic) | Critical | Milestone 9 | **PASSED** | `scripts/prepare_official_margin_history_snapshot.py`; verified `clean_margin_data` output went from 2 to 1,831 stocks/day on a real date; `tests/unit/test_prepare_official_margin_history_snapshot.py` (7 tests) |
| **M9-02** | Convert new-gainer/continued-momentum absolute thresholds to rolling quantiles (SPEC_ADDENDUM B-1.3), no-lookahead guaranteed | Critical | Milestone 9 | **PASSED (3 of 17 thresholds, PRELIMINARY)** | `src/threshold_calibration.py`; `tests/unit/test_threshold_calibration.py` (17 tests) + `tests/leakage/test_threshold_calibration_no_future_function.py` (4 tests); only `new_gainer.min_score`/`prev_score_max` and `continued_momentum.min_score` calibrated this round (n=28 trading days, honestly disclosed as small-sample) |
| **M9-03** | Calibration wired opt-in, zero effect on existing callers unless explicitly enabled | Critical | Milestone 9 | **PASSED** | `SignalDetector(use_calibrated_thresholds=False)` (default) byte-identical to pre-M9; `TestCalibratedThresholds::test_default_off_preserves_exact_pre_m9_behavior` + 3 more tests in `tests/unit/test_signal_detector.py` |
| **M9-04** | Historical batch pipeline rerun with real margin data, honest regression disclosure | Critical | Milestone 9 | **PASSED (regression found + disclosed, not hidden)** | 28/62 SUCCESS (was 60/62 at M5c-prep) — root-caused to a pre-existing (M3-disclosed) leaderboard reconciliation basis mismatch now firing at scale because FinMind OHLCV reached 100% coverage; confirmed NOT caused by this session's margin work (`calculate_quality_score` never reads `df_margin`); out of authorized scope to fix, disclosed in `docs/Milestone_9_Calibration_Backtest_Report.md` §2 |
| **M9-05** | Backtest rerun with calibrated thresholds, honest before/after headline comparison | Critical | Milestone 9 | **PASSED (byte-identical headline, root-caused not assumed)** | Isolated clean 28-date backtest run before/after: B級 -11.28%/C級 -10.99% unchanged in both cases (`diff -q` confirms byte-identical event CSVs); root-caused to all 53 events first-firing on the cold-start day or an UNTRADABLE day, structurally outside calibration's reach this round — disclosed as a measurement ceiling, not a verdict on the calibration's merit |
| **M9-06** | Test suite: 419 baseline + new tests green, zero regressions | Critical | Milestone 9 | **PASSED** | 451/451 passed (419 baseline + 7 margin-bridge + 17 threshold-calibration + 4 leakage + 4 signal-detector-calibration); `loop/evidence/test_logs/pytest_calibration_run_log.txt` |
| **M10-01** | Remove generic C-grade fallback and enforce selective new-gainer grading | Critical | Milestone 10 | **PASSED** | `src/signal_detector.py::_grade_new_gainer`: explicit trigger/core/breadth/veto policy; A requires full evaluation; B requires 3 core + breadth; C requires rule-6 individual evidence or UAT-04 branch; otherwise 無訊號. |
| **M10-02** | Unevaluable and safety-veto semantics are fail-closed | Critical | Milestone 10 | **PASSED** | Unevaluable rules never enter `passed`; rules 7-10 block A/B when failed or unevaluable; boundary tests in `tests/unit/test_signal_detector.py`. |
| **M10-03** | Preserve UAT-04 single-stock protection | Critical | Milestone 10 | **PASSED** | `MIN_UP_STOCKS_FOR_SECTOR_SIGNAL=2` unchanged; single-stock rows cannot be A/B; existing and new UAT-04 tests pass. |
| **M10-04** | Continued-momentum selectivity | High | Milestone 10 | **PASSED** | Continued signal requires core rules 1 and 4; optional unevaluable rules 5/6 degrade confidence only; covered by targeted detector tests. |
| **M10-05** | Full 62-day calibrated/uncalibrated rerun after DQ gate repair | Critical | Milestone 10 | **PASSED** | Both modes 62/62 success; 2,857 signal rows per mode; events 495/631; 61 event dates; zero 2026-04-20 cold-start events; evidence `Quant-Agent/_workbench/out/moneyflow_62d_backtest_20260722/selectivity_backtest_summary.json`. |
| **M10-06** | Demonstrate calibration can affect measurable events | Critical | Milestone 10 | **PASSED (effect measurable, predictive value unproven)** | 704 daily signal-type changes; event CSVs are not byte-identical; C medians -2.7852%/-2.8833% versus -1.2338% momentum baseline; A/B have no realized samples. |
| **M10-07** | Regression-free test suite and report | Critical | Milestone 10 | **PASSED** | Targeted 25/25 (`loop/evidence/test_logs/pytest_signal_selectivity_targeted_run_log.txt`); full 463/463 (`loop/evidence/test_logs/pytest_signal_selectivity_run_log.txt`); report `docs/Milestone_10_Signal_Selectivity_Report.md`. |
| **M10-08** | Phase 1 paired calibration audit on fixed sector-day keys | Critical | Milestone 10 follow-up | **PASSED (selection effect isolated; OOS pending)** | `Quant-Agent/_workbench/tools/run_paired_calibration_audit.py`; 2,857/2,857 sector-days paired, 704 label changes, event overlap 264, uncalibrated-only 231, calibrated-only 367; 228 realized shared events have paired delta 0. Full suite after additive tests: 467/467 (`loop/evidence/test_logs/paired_calibration_full_pytest_log.txt`). |
| **M10-09** | Sample-out-of-time forward validation with frozen calibration | Critical | Milestone 10 follow-up | **INSUFFICIENT_OOS_DATA (correct stop)** | Availability gate executed; only 2026-07-20 is post-training, 0 mature dates with 10 subsequent scored dates, no performance headline emitted. Existing 7/20 signal files are date-presence evidence only and require a frozen-M10 rerun before measurement. Receipt `Quant-Agent/_workbench/out/moneyflow_62d_backtest_20260722/frozen_oos_validation_receipt.json`; rerun after new dates arrive. |
| **M11-01** | Detect weekday gaps across a calendar lookback; skip existing SUCCESS days | Critical | Auto-backfill late data | **PASSED** | `_find_backfill_gaps`; current dry-run found 2026-07-21/22 and excluded 2026-07-20 SUCCESS |
| **M11-02** | Official readiness probe (MI_INDEX + STOCK_DAY_ALL), ROC/Chinese date parsing, fail-closed | Critical | Auto-backfill late data | **PASSED** | Live samples `loop/evidence/raw_samples/mi_index_20260722T203706.json` and `stock_day_all_20260722T203706.json`; both self-report 1150721 |
| **M11-03** | Ready/deferred/holiday/idempotent/exception loop semantics; deferred does not write BLOCKED audit | Critical | Auto-backfill late data | **PASSED** | 12 hermetic tests in `tests/integration/test_auto_backfill.py`; full suite 487/487 |
| **M11-04** | CLI/backward compatibility and wrapper documentation | High | Auto-backfill late data | **PASSED** | `--date` and `--no-backfill` tests; `daily_run.ps1/.bat` preserved; Task Scheduler not modified |
| **M11-05** | Live E2E late-data retry and manual correction persistence | Critical | Auto-backfill late data | **PASSED** | Exact `daily_run.bat` rerun recovered TPEx 2026-07-21 via official date-addressable `afterTrading/dailyQuotes` (10,072 rows), wrote raw/bridge data, and generated `outputs/daily/MoneyFlow_Rotation_2026-07-21.xlsx`; batch exit 0. 2026-07-22 remains `DEFERRED_NOT_READY`. Evidence: `loop/evidence/manual_daily_run_after_historical_tpex_20260722.log`, `loop/evidence/fetch_receipts/fetch_receipt_2026-07-21.json`, full suite 487/487. |
